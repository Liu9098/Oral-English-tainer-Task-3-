# 实时口语训练台 · Render 部署说明书（小白网页操作版）

> 目标：把"与 AI 客户 Mr. Brown 实时英语通话 + 自动生成课堂质检报告"的训练台，
> 部署成一个**长期可访问、带 HTTPS、麦克风可用**的公网网址（如 `https://trainer.onrender.com`）。
> 全程**网页点击、零命令行**。

---

> ⚠️ **密钥安全提醒**
> 本部署包的密钥只应出现在 **Render 后台的 Environment Variables** 中，
> **绝不能**出现在任何上传 GitHub 的文件里（否则 GitHub 会拦截并报"Secret scanning"）。
> 真实密钥已写在本包根目录的 `本地密钥-勿上传.txt` 中，**此 txt 不上传 GitHub**。

---

## 一、这个包里有什么（上传哪些文件）

| 文件 / 目录 | 放哪 | 说明 |
|---|---|---|
| `server.py` | 仓库根目录 | 已适配云部署：监听 `0.0.0.0`、端口读 `$PORT`、仅用 Python 标准库 |
| `requirements.txt` | 仓库根目录 | 空文件，无需装依赖 |
| `runtime.txt` | 仓库根目录 | 指定 Python 版本 |
| `static/index.html` | `static/` 目录 | 训练台页面 |
| `static/app.js` | `static/` 目录 | 前端逻辑（已设为 trainer 模式，无需学生填密钥） |
| `static/style.css` | `static/` 目录 | 样式 |
| `static/assets/*` | `static/assets/` 目录 | 场景图、头像、视频等素材（4 个文件） |
| `部署说明书.md` | 仓库根目录 | 本文件 |
| `本地密钥-勿上传.txt` | **不上传** | 仅在本地查看，用于复制密钥到 Render 后台 |

---

## 二、原理（一句话）

GitHub 存代码 → Render 连 GitHub 拉代码、自动跑 `server.py`、分配一个 https 网址 →
浏览器打开网址即可用，麦克风自动可用（https 是浏览器允许麦克风的前提）。

---

## 三、第 1 步：上传到 GitHub（网页操作）

1. 打开 [github.com](https://github.com) 并登录，**右上角 `+` → `New repository`**。
2. Repository name 填 `trainer`，选 **Public**，其他默认，**`Create repository`**。
3. 在仓库页面，点 **`Add file` → `Upload files`**，按下面"位置"逐个上传：

   **根目录（直接拖）：**
   - `server.py`
   - `requirements.txt`
   - `runtime.txt`
   - `部署说明书.md`

   **`static/` 目录：** 先在仓库里点 `Add file → Create new file`，文件名填 `static/.gitkeep`（自动建出 `static` 文件夹），提交；再进入 `static` 文件夹，点 `Upload files`，拖入 `index.html`、`app.js`、`style.css` 三个文件。

   **`static/assets/` 目录：** 同理先进 `static/assets/` 文件夹，再 `Upload files` 拖入 4 个素材文件。

4. 拉到底点 **`Commit changes`**。

> ⚠️ **网页 GitHub 上传铁律（必看，否则文件会传到错误位置）：**
> ① **先点进目标子目录**（如要传 `static/` 里的文件，先进 `static/` 再传）；
> ② **只拖单个/多个文件，绝不拖整个文件夹**（拖文件夹会在里面再套一层同名目录）。

---

## 四、第 2 步：Render 部署（网页操作）

1. 打开 [render.com](https://render.com) → 右上角 **`Sign Up`** → 选 **`Continue with GitHub`** → 弹窗点绿色 **`Authorize`**（免信用卡、免新密码）。
2. 登录后，点右上角 **`New +`** → 下拉选 **`Web Service`**。
3. 在 *Connect a repository* 选 **GitHub**，找到 `trainer` 仓库，点 **`Connect`**。
4. 填配置（照抄）：

   | 项目 | 填什么 |
   |---|---|
   | Name | `trainer` |
   | Region | **Singapore**（离中国近） |
   | Branch | `main` |
   | Runtime | **Python 3** |
   | Build Command | `pip install -r requirements.txt` |
   | Start Command | `python server.py` |
   | Instance Type | **Free**（免费档） |

5. 页面往下翻到 **`Environment Variables`**，点 **`Add Environment Variable`**，共加 3 次，每次一对：

   | KEY | VALUE（粘贴你本地保存的真实值） |
   |---|---|
   | `VOLC_API_KEY` | 你的火山语音 API Key（UUID 格式） |
   | `ARK_API_KEY` | 你的火山 Ark API Key（ark- 开头） |
   | `VOLC_REPORT_MODEL` | 你的推理接入点 ID（ep- 开头） |

   真实值见本包里的 `本地密钥-勿上传.txt`，**不要**把这个 txt 传到 GitHub，只填进 Render 后台即可。
   （KEY 名字必须与上表完全一致；VALUE 直接复制粘贴，不要加引号。）

6. 拉到最底点 **`Create Web Service`** → 等 **2–5 分钟**自动构建（可点 `Logs` 看进度）→ 顶部变绿色 **`Live`** → 页面给出网址 `https://trainer.onrender.com`。

---

## 五、验证是否成功

1. 浏览器打开 `https://trainer.onrender.com`（**第一次会慢 30–60 秒**，免费档"休眠唤醒"，属正常）。
2. 点大麦克风，允许浏览器使用麦克风。
3. 用英语和 Mr. Brown 聊几句（如解释他的体检指标）。
4. 点"结束并生成报告"，应弹出课堂质检报告（Accuracy / Care / Culture）。
5. 全通 = 部署成功 ✅

---

## 六、常见问题

- **第一次打开很慢 / 白屏**：免费档 15 分钟无访问就休眠，唤醒需 30–60 秒。上课前先点开一次即可。想永不停需升级 Render Basic（$7/月）。
- **能开页面但语音报错 / 报告报错**：99% 是 3 个密钥名字或值抄错。回 Render 后台 `Environment` 核对，改完点 `Manual Deploy` 重新部署。
- **部署失败、Logs 报端口错误**：不应发生（server.py 已改读 `$PORT`）。若仍报，检查 Start Command 是否为 `python server.py`。
- **WebSocket / 麦克风连不上**：确认网址是 `https://` 开头（Render 默认就是）。

---

## 七、接入课程平台（方案 B：iframe 内嵌）

1. 拿到训练台网址后，打开课程平台副本 `course-workbench-trainer/js/app.js`。
2. 找到这一行，把空字符串改成你的网址：
   ```js
   const TRAINER_URL_PUBLIC = 'https://trainer.onrender.com/';
   ```
3. 把课程平台副本也部署出去（GitHub Pages 或 Render 静态站点均可），原课程平台网址 `e006250c…agentos-app.net` 完全不动、零影响。

---

## 八、本地调试（可选）

本机仍可像以前一样跑（不受云端影响）：
```
cd web_duplex_demo
:: 把下面三个值改成 本地密钥-勿上传.txt 里的真实值（不要写进任何仓库文件）
set VOLC_API_KEY=<你的火山语音API Key>
set ARK_API_KEY=<你的火山Ark API Key>
set VOLC_REPORT_MODEL=<你的推理接入点ID>
python server.py
```
打开 `http://127.0.0.1:8765/`（本地默认端口）。
